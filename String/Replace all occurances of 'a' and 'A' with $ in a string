#include<stdio.h>
#include<string.h>
int main()
{
	char str[100];
	int i;
	printf("Enter a string: ");
	gets(str);
	
	for(int i = 0; i < strlen(str); i++) {
		if(str[i] == 'a' || str[i] == 'A') {
			str[i] = '$';
		}
	}
	
	printf("String after replacement: %s\n" , str);
	
	return 0;
}