#include<stdio.h>
int main()
{
	int no = 28;
	int sum = 0;
	int i = 1;
	
	while (i < no) {
		if(no % i == 0) {
			sum = sum + i;
		}
		i++;
	}
	
	if (sum == no) {
		printf("%d is a perfect number\n" , no);
	} else {
		printf("%d is not a perfect number\n" , no);
	}
}