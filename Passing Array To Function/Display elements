#include <stdio.h>

void display(int arr[], int size) {
    int i;
    for(i = 0; i < size; i++) {
        printf("%d ", arr[i]);
    }
}

int main() {
    int a[5] = {10, 20, 30, 40, 50};

    display(a, 5); // passing array a

    return 0;
}