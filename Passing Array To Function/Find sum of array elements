#include <stdio.h>

int sum(int arr[], int n) {
    int i, s = 0;
    for(i = 0; i < n; i++) {
        s += arr[i];
    }
    return s;   // return total sum
}

int main() {
    int a[5] = {5, 10, 15, 20, 25};

    int result = sum(a, 5);

    printf("Sum = %d", result);

    return 0;
}