#include<stdio.h>
int main()
{
	int no = 121;
	int first_digit = no / 100;
	int last_digit = no % 10;
	if(first_digit == last_digit){
		printf("%d is a palindrome number \n", no);
	}
	else
	{
		printf("%d is not a palindrome number \n", no);
	}
}