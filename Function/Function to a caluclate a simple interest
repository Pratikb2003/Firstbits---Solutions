class SimpleInterest {

    static double calculateSI(double p, double r, double t) {
        return (p * r * t) / 100;
    }

    public static void main(String[] args) {
        double si = calculateSI(1000, 5, 2);
        System.out.println("Simple Interest = " + si);
    }
}